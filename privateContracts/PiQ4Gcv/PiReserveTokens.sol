pragma solidity v.0.8;

contract PiReserveAsset(store, address, uimt256){


async function store(){
$PI
$BTC
$WLFI
$ETH,
$STOCKS
$GOLD,
$OIL,
$REALESTATE,
$WHEAT,
($ASSET1, $ASSET2, USDDenomination);
}

function transfer(sender, recipient, uint256){
msg.sender(uint256) += msg.recipient[balance];

}

function swap(){
swap.PiDefi;
}

}
