pragma solidity v.0.8;


contract EnterpriseWorkforce(associate, job, uint256){

associate:(
account information,
address,
selectedCurrency,)

job:(
hourlyWage,
schedule,
hoursThisCycle,
hoursThisCycle * hourlyWage = CycleEquivalent,
CycleEquivalent,)

uint256:(
selectedCurrency,
CycleEquivalent ÷ selectedCurrency[price] = amount of ($selectedCurrency) to receive), )
}
