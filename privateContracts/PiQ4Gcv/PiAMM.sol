pragma solodoty v.0.8;

import chainlink pricefeeds;
import {verifiedTokens, verifiedChains} from InstilledaInteroperability.sol;
import {PiDex[Pi(native)], PiDex[Ethereum], PiDex[Binance], PiDex[Stellar], and etc from PiDex & add to InstilledInteroperability too;

(chain, token):(
contract addresses,
token name,
token symbol,
token decimal,
token circulating supply,
token total supply,
token max suplply,
token marketcap,
token marketcap ÷ circulating supply token price,
token price,



contract PiAMM (msg.sender, verifiedToken, uint256){

function store(){}

function

}
