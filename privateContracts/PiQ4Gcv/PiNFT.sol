pragma solidity v0.8;

contract PiNFT(msg.sender, nft, uint256) {

msg.sender:(
function buy(nft, uint256);),

nft:(
creator address,
owner address,
price,),

uimt256:(
price,
stocks,
gold);


dividends(owner, creator, nft marketcap);
}
