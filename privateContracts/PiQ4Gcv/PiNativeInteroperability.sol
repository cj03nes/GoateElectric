pragma solidity v0.8;

contract PiNativeInteroperability (uint256, mediator, uint256){

uint256:( ),
mediator:(
$PI,
$USDC,
$XLM,);

uint256 => $PI => uint256;
uint256 => $XLM => uint256;
uint256 => $USDC => uint256;

}
