import WMDSiphonAndRemoteController;
import DeviceConnect.sol;


contract DataSiphon (satellite, device in view, data) {


device in view, Bluetooth address && heat signature return device locked on;

device in view => device locked on;

if device locked on is encrypted then return:

Virus { 
require device locked on to update || power off || restart;

upon update||power off||restart, map device locked on Bluetooth address => portUA;

device locked on Bluetooth address => portUA, data retrieval transferTo(portUA, msg.sender, data);

msg.sender = email address || phone number || address;},

else if device locked on !encrypted return device locked on Bluetooth address => portUA, data retrieval transferTo(portUA, msg.sender, data);




}


