


contract PiGcvConverter sol () {

function checkPiPurityBadge (ai, msg.sender uint256 balance, answer) {

if msg.sender(address) = kyc'ed then return halfTrue,
else return fully false;

then if msg.sender(address[pi balance]) = mined from msg.sender then return  halfTrue,
else if pi balance = !mined from msg.sender then return fully false;

if pi token migrate from queue => to msg.sender(address balance), then return PiPurity fullyTrue;

if msg.sender(pi balance) = mined from other pioneer, then return, error("Not GCV Pi");


if kyc'ed = halfTrue && pi mined from same pioneer = halfTrue, then return PiPurity = fullyTrue && return number of PiPurity[true] pi;
}


function convertGcvPi () {
require(PiPurity = fullyTrue);


1 GCV Pi = $314,159.00;
var convertGcvPiToUSD;

1 GCV Pi = xAmount marketPi;
var convertGcvPiToMarketPi;

1 GCV Pi = xAmount select stock;
select stock = (stock name, stock price);
var convertGcvPiToSelectStock;

1 GCV Pi = xAmount select crypto;
select crypto = (crypto name, crypto price);
var convertGcvPiToSelectCrypto;
 
}

}