// import pi auth sdk
import pi tokens & verifiedassets



contract Pi overlay marketplace (msg.sender, marketplace, uint256 pi) {

var p2pListing;

var Amazon marketplace listing;
*/ like an ad except you can paywithpi and buy the product through the site or dropshipping /*

function createPiOverlayWebsite (marketplace, pi checkout, msg.sender(balance)(cart) uint255 pi){

let msg.sender access "marketplace" in Pi Browser;
upon msg.sender, click cart;

add CheckoutWithPi Modem;
CheckoutWithPi Modem => Pi Browser, msg.sender cart;

Pi Browser, msg sender cart => PayWithPi || PayWithCrypto || PayWithCard;

}

}


}