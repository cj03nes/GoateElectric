pragma solidity v.0.8;

import ZeropointWifi;

contract BankingATM (card, user, uint256){

function deposit fiat(card, fiat, uint256){

if msg.sender deposit fiat,
then the $amount in $USD = the usd $amount in select crypto;
}

function withdraw fiat(card, fiat, uint256){

if msg.sender withdraw fiat,
then the usd $amount in msg.sender[balance] = the usd $amount to dispense;
}


function sell gold(weight, identification, uint256){

if msg.sender sells gold,
then get:(
gold[weight],
gold[purity],
gold[pricefeed],

gold[value in $USD]{}

}

function buy gold(uint256 insert, identification, uint256 dispense) {

uint256 insert:(
card,
fiat,
),
identification:(
id,
license,
),
dispense:(
gold,
silver,
copper,
),
}

}
