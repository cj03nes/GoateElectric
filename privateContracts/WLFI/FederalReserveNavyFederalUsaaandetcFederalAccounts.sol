pragma solidity v.0.8;

import WalletStandard;

contract upgrade(uint256, account, uint256);

*/
upgrade(uint256) ($BTC, $USD, USDDenomination) => ($BTC, $STOCKS, $USDDenomination) & etc;
/*

banks:(
($USDC, $BTC, USDDenomination),
($USDT, $GOLD, USDDenomination),
($USD1, $STOCKS, USDDenomination),
($1, $BTC, USDDenomination),

uint256:(
usdc,
usdt,
usd1,
digital dollar,
),
